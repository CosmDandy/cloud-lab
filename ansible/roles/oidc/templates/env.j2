# Managed by ansible (roles/oidc). Правки на хосте будут перезаписаны.
APP_URL=https://{{ oidc_domain }}
PORT={{ oidc_port }}
TRUST_PROXY={{ oidc_trust_proxy }}
DB_CONNECTION_STRING={{ oidc_db_connection_string }}
ENCRYPTION_KEY={{ oidc_encryption_key }}
ANALYTICS_DISABLED={{ oidc_analytics_disabled | lower }}
