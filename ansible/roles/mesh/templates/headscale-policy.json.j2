{
  // Managed by ansible (roles/mesh).
  // В 0.29 этот ключ убран из config.yaml и живёт только здесь.
  "randomizeClientPort": false,

  "grants": [
    // Плоский доступ внутри сети. Внимание: в 0.29 "*" означает только узлы
    // tailnet (CGNAT + ULA), а не 0.0.0.0/0, как было в 0.28.
    // TODO: сегментировать серверы и личные устройства.
    {
      "src": ["*"],
      "dst": ["*"],
      "ip": ["*"]
    }
  ]
}
