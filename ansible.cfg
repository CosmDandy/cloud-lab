[defaults]
# Repo-root-relative paths so ansible commands can be run from the repo root.
# Если запускаешь из ansible/ — задействуется ansible/ansible.cfg.
roles_path = ansible/roles
inventory = ansible/inventory/local.yml
collections_paths = ansible/collections
